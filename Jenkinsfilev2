pipeline {
  agent any
   stages {
    stage ('Build') {
      steps {
        sh '''#!/bin/bash
        py setup2.py
        '''
     }
   }
 stage ('Deploy') {
  steps {
     sh '''#!/bin/bash
     pip install paramiko
     pip install rich
    python setup.py
    '''
}
}
  }
 }
